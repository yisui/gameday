#' R package: gameday
#'
#' This package provides an easy way to access the data for NHL teams through R.
#'
#' There are two functions \code{gday(team, date)} and \code{scores(date)}
#' in the gameday package.
#' \code{gday(team, date)} gives an option to check whether a specified NHL team has a
#' game on a specified date. \code{scores(date)} gives the option to display the scores of
#' all the games on a specified date as a \code{data.frame}. \code{scores(date)} also
#' provides the information about whether the game can be watched on the "gamecenter".
#'
#' @docType package
#' @name gameday
#'
NULL

